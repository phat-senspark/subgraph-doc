{
  "name": "bhero",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node http://localhost:8020/ test/bhero",
    "create-local": "graph create --node http://localhost:8020/ test/bhero",
    "remove-local": "graph remove --node http://localhost:8020/ test/bhero",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs  http://localhost:5001 test/bhero"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.31.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
